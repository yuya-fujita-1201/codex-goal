# Codex Goal

Codex CLI を非対話モードでターン実行し、最初に設定したゴールが達成されるまで
新しい Codex セッションを継続起動する Electron アプリです。

アプリ本体は `app/` にあります。

```bash
cd app
npm install
npm run dev
```

詳細は [app/README.md](app/README.md) を参照してください。
